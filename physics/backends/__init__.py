"""
GPU physics backends — Warp kernels and shared static data.

The Warp backend (``warp/``) provides all GPU kernels used by ``GpuEngine``.
``static_data.py`` converts ``MergedModel`` into flat arrays for GPU upload.
"""
